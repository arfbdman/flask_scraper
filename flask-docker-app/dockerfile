# Step 1: Use an official Python image as the base
FROM python:3.9-slim

# Step 2: Set the working directory in the container
WORKDIR /app

# Step 3: Copy necessary files from your computer to the container
COPY requirements.txt requirements.txt
COPY app.py app.py
COPY templates/ templates/

# Step 4: Install Python dependencies
RUN pip install --no-cache-dir -r requirements.txt

# Step 5: Expose Flask's default port (5000) so others can access it later
EXPOSE 5000

# Step 6: Start the Flask app
CMD ["python", "app.py"]